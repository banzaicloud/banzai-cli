linters:
    enable-all: true
    disable:
        - maligned

        # TODO: fix these
        - dupl
        - gochecknoglobals
        - lll
        - deadcode
        - golint
        - gochecknoinits
        - gofmt
        - gocritic
        - megacheck
        - errcheck
        - goimports
        - gocyclo

linters-settings:
    golint:
        min-confidence: 0.1
